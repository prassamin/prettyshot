/**
 * Annotation stroke element constants.
 */

/** Distance (canvas px) from center that triggers the center-snap lock. */
export const SNAP_PX = 16;

/** Extra width added to the invisible hit-test path so strokes are easy to grab. */
export const HIT_TEST_PADDING = 6;

/** Minimum hit-test path width (even for thin pen strokes). */
export const MIN_HIT_TEST_WIDTH = 10;
