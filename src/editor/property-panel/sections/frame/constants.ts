/**
 * Frame picker — layout & preview constants.
 */

 /** Virtual width of the empty-state drop prompt inside device previews. */
export const DEVICE_FRAME_EMPTY_VIRTUAL_WIDTH = 280;

/** Browser tile preview sizes (rendered at virtual width, scaled down). */
export const BROWSER_TILE_PREVIEW_WIDTH = 112;
export const BROWSER_TILE_PREVIEW_VIRTUAL_WIDTH = 240;
