export const authClient = {};
export const useSession = (): { data: { user: { image?: string | null; name?: string | null; email?: string | null } } | null } => ({ data: null });
export const signOut = () => {};

